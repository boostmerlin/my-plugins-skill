interface:
  display_name: "My Skills"
  short_description: "Sync, update, and audit your trusted skill catalog"
  default_prompt: "Use $my-skills-skill to sync my environment to the default profiles."

policy:
  allow_implicit_invocation: true
