interface:
  display_name: "My Plugins"
  short_description: "Manage trusted skills and external plugins"
  default_prompt: "Use $my-plugins-skill to sync my environment to the default profiles."

policy:
  allow_implicit_invocation: true
